import React from "react";
import LinearProgress from "@material-ui/core/LinearProgress";

const LoadingState = (): React.FunctionComponentElement<{}> => <LinearProgress variant="query" />;

export default LoadingState;
